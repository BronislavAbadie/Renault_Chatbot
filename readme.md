Rag-based chatbot built as a project for the Renault group.

Install piper separately. Install the desired LLM that will go in the pipeline separately.

To run the app, activate the environment and run run_app.py or directly execute run_app.bat on windows.
